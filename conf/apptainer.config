apptainer {
    enabled = true
    autoMounts = true
    cacheDir = "${projectDir}/containers"
    runOptions = params.container_bind_paths.collect { "--bind '${it}'" }.join(' ')
}

singularity {
    enabled = false
}
