process {
    executor = 'slurm'

    resourceLimits = [
        memory: params.max_memory,
        cpus: params.max_cpus,
        time: params.max_time
    ]

    clusterOptions = ''
}

executor {
    queueGlobalStatus = true
}
